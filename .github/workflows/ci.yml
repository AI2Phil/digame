name: CI Pipeline

on:
  push:
    branches: [main]
  pull_request:

jobs:
  frontend-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install frontend dependencies
        working-directory: ./frontend
        run: npm ci

      - name: Build frontend
        working-directory: ./frontend
        run: npm run build:ci

  backend-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install backend dependencies
        working-directory: ./backend
        run: npm ci

      - name: Build backend
        working-directory: ./backend
        run: npm run build || true

  # E2E tests temporarily disabled - uncomment when ready to re-enable
  # e2e-test:
  #   needs: [frontend-build, backend-build]
  #   runs-on: ubuntu-latest
  #   strategy:
  #     matrix:
  #       browser: [chromium]
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4
  #     # ... (E2E test steps commented out for now)
