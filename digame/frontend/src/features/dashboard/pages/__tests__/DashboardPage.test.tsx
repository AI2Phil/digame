import React from 'react';
// import { render, screen } from '@testing-library/react';
// import DashboardPage from '../DashboardPage';

// Mock hooks for testing page component
// jest.mock('../hooks/useDashboardData', () => ({
//   useProductivityChartData: () => ({ data: { title: 'P Chart', data:[] }, isLoading: false, error: null }),
//   useActivityBreakdownData: () => ({ data: { title: 'A Breakdown', data:[] }, isLoading: false, error: null }),
//   useProductivityMetricsData: () => ({ data: { title: 'P Metrics', metrics:[] }, isLoading: false, error: null }),
//   useRecentActivitiesData: () => ({ data: { title: 'R Activities', activities:[] }, isLoading: false, error: null }),
// }));

describe('DashboardPage', () => {
  it('renders all dashboard components', () => {
    // render(<DashboardPage />);
    // expect(screen.getByText(/Digame Dashboard/i)).toBeInTheDocument();
    // expect(screen.getByText(/P Chart/i)).toBeInTheDocument(); // Based on mock
    expect(true).toBe(true); // Placeholder
  });
});
